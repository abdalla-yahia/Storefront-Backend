CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
CREATE TABLE products (id uuid DEFAULT uuid_generate_v4() PRIMARY KEY, name VARCHAR(250) NOT NULL,price VARCHAR(200) NOT NULL,count VARCHAR(100) NOT NULL, total_price VARCHAR(250) NOT NULL, password VARCHAR(250) NOT NULL,email VARCHAR(250) UNIQUE NOT NULL);